import XCTest
@testable import FNMOTPFields

final class FNMOTPFieldsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FNMOTPFields().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
